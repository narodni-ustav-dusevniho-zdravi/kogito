import { ContentItem } from '../../../data';
import { RelaxationIntro, RelaxationBreathing } from '../../commonRelaxation';

const items: ContentItem[] = [RelaxationIntro, RelaxationBreathing];

export default items;
