import styled from 'styled-components/native';

type Progress = {
  progress: number;
};

const S = {
  Wrapper: styled.View`
    width: 100%;
    padding: 0;
    margin-bottom: 0;
  `,
};

export default S;
