import React, {FC} from 'react';
import {Card, CardContent, CardHeader} from '@material-ui/core';

const Dashboard: FC = () => {
  return (
    <Card>
      <CardHeader title="Kogito backoffice" />
      <CardContent></CardContent>
    </Card>
  );
};

export default Dashboard;
